# CLAUDE.md — diplomacy-briefing

外交モーニング・ブリーフィングの見出しを配信する仕組み。生成タスクが
`gaiko-brief.json`（および確認用 `data.json`）を出力し、ブラウザ画面
（`index.html`）と iPhone ウィジェット（`gaiko-brief-widget.js`）が読み込む。

## ブリーフィング生成の必須ルール

1. **外務省（mofa.go.jp）を毎回必ず参照する。** これは絶対要件。
   - 生成のたびに、外務省の「報道発表（プレスリリース）」「外務大臣会見記録」
     「大臣談話」「各会談記録」を確認する。
   - すべての日本関連項目を外務省ソースに寄せる必要はないが、**参照は必須**で、
     可能なものは一次ソースとして `source` に採用し、
     **最低1件は外務省を出典に含める**こと。
   - 環境制約：この環境では mofa.go.jp への直接アクセス（WebFetch/curl）は
     egress ポリシーで 403 になる。ただし **WebSearch 経由なら外務省ページの
     本文を読み取れる**ので、そこから内容を確認して引用してよい。

2. **出典必須・確度必須。** 各 item は `source`(URL)・`sourceName`(媒体)・
   `confidence`(`高`/`中`/`低`) を持つ。裏取りできない項目は載せない。

3. **主要紙・一次情報を優先。** 各国の主要新聞社・通信社、および政府・国際機関の
   一次発表（外務省・各国外務省・EU理事会・国連・WMO など）を優先する。
   日付を突き合わせ、対象日（当日〜直近）の事実のみ採用する。

4. **信頼度の基準**
   - `高`：複数の主要紙または一次情報で、日付を含め一致
   - `中`：単独ソース／当日以外・細部に幅がある
   - `低`：未確認（原則掲載しない）

## データスキーマ（`data.json` / `gaiko-brief.json`）

```json
{
  "updated": "ISO8601",
  "url": "",
  "items": [
    {
      "region": "地域名",
      "tag": "小分類",
      "title": "見出し",
      "priority": true,
      "confidence": "高",
      "sourceName": "媒体名",
      "source": "https://出典URL"
    }
  ]
}
```

- `region` はセクション見出し。表示順は `index.html` の `REGION_ORDER`
  （日本 / ASEAN / 中国 / 朝鮮半島 / 西アジア / 中東 / 東アフリカ / 西アフリカ /
  西/東/北/南ヨーロッパ / ロシア / アメリカ / 北米 / 中南米 / 大洋州）。
  データに存在する地域のみ表示。区分外は末尾、未指定は「その他」。
- `priority: true` は各地域内で先頭に、赤＋★で強調。
- `index.html` は各見出し下に確度バッジと「出典: 媒体名」リンクを表示する。
- `data.json`（ブラウザ確認用サンプル）と `gaiko-brief.json`（ウィジェット／
  Drive 配信用）は同一スキーマ。生成時は両方を更新する。

## ローカル確認

`python3 -m http.server 8000` → `http://localhost:8000/`（`fetch` を使うため）。
